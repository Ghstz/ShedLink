<UserControl x:Class="Shed_Security_AP.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:Shed_Security_AP.Core"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">

    <Grid>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>

        <!-- ════════════════════ Metric Cards ════════════════════ -->
        <UniformGrid Grid.Row="0" Grid.ColumnSpan="2"
                     Rows="1" Columns="3"
                     Margin="0,0,0,12">

            <!-- TPS Card -->
            <Border Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="8"
                    Padding="15"
                    Margin="0,0,8,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.2" ShadowDepth="2" Direction="270" />
                </Border.Effect>
                <Grid VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <TextBlock Text="TPS"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Tps}"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="12"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <lvc:CartesianChart Grid.Row="1"
                                        Series="{Binding TpsSeries}"
                                        XAxes="{Binding HiddenXAxes}"
                                        YAxes="{Binding HiddenYAxes}"
                                        TooltipPosition="Hidden"
                                        Height="40"
                                        Margin="0,6,0,0" />
                </Grid>
            </Border>

            <!-- RAM Card -->
            <Border Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="8"
                    Padding="15"
                    Margin="4,0,4,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.2" ShadowDepth="2" Direction="270" />
                </Border.Effect>
                <Grid VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <TextBlock Text="RAM"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding RamUsage}"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="12"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <lvc:CartesianChart Grid.Row="1"
                                        Series="{Binding RamSeries}"
                                        XAxes="{Binding HiddenXAxes}"
                                        YAxes="{Binding HiddenYAxes}"
                                        TooltipPosition="Hidden"
                                        Height="40"
                                        Margin="0,6,0,0" />
                </Grid>
            </Border>

            <!-- Uptime Card -->
            <Border Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="8"
                    Padding="15"
                    Margin="8,0,0,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.2" ShadowDepth="2" Direction="270" />
                </Border.Effect>
                <Grid VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <TextBlock Text="UPTIME"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Uptime}"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="12"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <!-- Server Power Buttons -->
                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,8,0,0">
                        <Button Content="Restart"
                                Command="{Binding RestartServerCommand}"
                                Style="{StaticResource AccentButton}"
                                Height="28"
                                FontSize="11"
                                Padding="10,0"
                                Margin="0,0,6,0" />
                        <Button Content="Stop"
                                Command="{Binding StopServerCommand}"
                                Style="{StaticResource AccentButton}"
                                Height="28"
                                FontSize="11"
                                Padding="10,0" />
                    </StackPanel>
                </Grid>
            </Border>
        </UniformGrid>

        <!-- ════════════════════ Live Console ════════════════════ -->
        <Grid Grid.Row="1" Grid.Column="0"
              Margin="0,15,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Console output -->
            <Border Grid.Row="0"
                    Background="{StaticResource InputBackgroundBrush}"
                    BorderBrush="{StaticResource BorderSubtleBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.2" ShadowDepth="2" Direction="270" />
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0"
                                Orientation="Horizontal"
                                Margin="0,0,0,10">
                        <TextBlock Text="&#xE756;"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   FontSize="14"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Live Server Console"
                                   Style="{StaticResource HeaderStyle}"
                                   FontSize="16" />
                    </StackPanel>

                    <!-- Quick Action Macros -->
                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <StackPanel.Resources>
                            <Style x:Key="QuickActionBtn" TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                                <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
                                <Setter Property="FontSize" Value="13" />
                                <Setter Property="Width" Value="34" />
                                <Setter Property="Height" Value="30" />
                                <Setter Property="Margin" Value="0,0,6,0" />
                                <Setter Property="Cursor" Value="Hand" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Bd"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="5"
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource BorderSubtleBrush}" />
                                                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}" />
                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </StackPanel.Resources>

                        <Button Content="&#xE706;"
                                Command="{Binding TimeMorningCommand}"
                                ToolTip="Set time to morning"
                                Style="{StaticResource QuickActionBtn}" />
                        <Button Content="&#xE9CA;"
                                Command="{Binding ClearWeatherCommand}"
                                ToolTip="Clear weather"
                                Style="{StaticResource QuickActionBtn}" />
                        <Button Content="&#xE74E;"
                                Command="{Binding SaveWorldCommand}"
                                ToolTip="Save world"
                                Style="{StaticResource QuickActionBtn}" />
                        <Button Content="&#xE74D;"
                                Command="{Binding ClearEntitiesCommand}"
                                ToolTip="Clear item drops"
                                Style="{StaticResource QuickActionBtn}" />
                    </StackPanel>

                    <ScrollViewer Grid.Row="2"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  core:AutoScrollBehavior.AutoScroll="True">
                    <ItemsControl ItemsSource="{Binding ConsoleLines}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontFamily="Cascadia Mono, Consolas, Courier New"
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Margin="0,2" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Command input row -->
            <Grid Grid.Row="1" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBox}" />

                <Button Grid.Column="1"
                        Content="Send"
                        Command="{Binding SendCommand}"
                        Style="{StaticResource AccentButton}"
                        Width="80"
                        Margin="8,0,0,0" />
            </Grid>
        </Grid>

        <!-- ════════════════════ Player List ════════════════════ -->
        <Border Grid.Row="1" Grid.Column="1"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16"
                Margin="0,15,0,0">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.2" ShadowDepth="2" Direction="270" />
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            Orientation="Horizontal"
                            Margin="0,0,0,12">
                    <TextBlock Text="&#xE716;"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0" />
                    <TextBlock Text="Players Online"
                               Style="{StaticResource HeaderStyle}"
                               FontSize="16" />
                </StackPanel>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Players}"
                                    x:Name="PlayerList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource InputBackgroundBrush}"
                                        CornerRadius="6"
                                        Padding="12,10"
                                        Margin="0,0,0,6"
                                        Tag="{Binding DataContext,
                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                                    <Border.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Kick Player"
                                                      Command="{Binding PlacementTarget.Tag.KickPlayerCommand,
                                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext,
                                                                         RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="Ban Player"
                                                      Command="{Binding PlacementTarget.Tag.BanPlayerCommand,
                                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext,
                                                                         RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <MenuItem Header="Mute Player"
                                                      Command="{Binding PlacementTarget.Tag.MutePlayerCommand,
                                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext,
                                                                         RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                            <Separator />
                                            <MenuItem Header="Inspect Player"
                                                      Command="{Binding PlacementTarget.Tag.InspectPlayerCommand,
                                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                                      CommandParameter="{Binding PlacementTarget.DataContext,
                                                                         RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        </ContextMenu>
                                    </Border.ContextMenu>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0"
                                                    VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontSize="13" />

                                            <!-- Health bar -->
                                            <ProgressBar Style="{StaticResource ModernProgressBar}"
                                                         Height="4"
                                                         Margin="0,4,0,0"
                                                         Minimum="0"
                                                         Maximum="{Binding MaxHealth}"
                                                         Value="{Binding Health, Mode=OneWay}"
                                                         Foreground="#E11D48"
                                                         ToolTip="{Binding Health, StringFormat='HP: {0:F0}'}" />

                                            <!-- Satiety bar -->
                                            <ProgressBar Style="{StaticResource ModernProgressBar}"
                                                         Height="4"
                                                         Margin="0,2,0,0"
                                                         Minimum="0"
                                                         Maximum="1500"
                                                         Value="{Binding Satiety, Mode=OneWay}"
                                                         Foreground="#D97706"
                                                         ToolTip="{Binding Satiety, StringFormat='Satiety: {0:F0}'}" />
                                        </StackPanel>

                                        <TextBlock Grid.Column="1"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   VerticalAlignment="Center"
                                                   Margin="12,0,0,0">
                                            <Run Text="{Binding Ping, Mode=OneWay}" /><Run Text=" ms" />
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>

    <!-- ════════════════════ Inspect Modal Overlay ════════════════════ -->
    <Border Background="#CC000000"
            Visibility="{Binding IsInspectModalOpen, Converter={StaticResource BoolToVisConverter}}"
            MouseDown="ModalOverlay_MouseDown">
        <Border Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="24"
                Width="380"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" Opacity="0.5" ShadowDepth="4" Direction="270" />
            </Border.Effect>
            <Grid DataContext="{Binding SelectedPlayerDossier}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="&#xE77B;"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="18"
                               Foreground="{StaticResource AccentBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0" />
                    <TextBlock Text="{Binding PlayerName}"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="18"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                </StackPanel>

                <!-- Dossier Fields -->
                <StackPanel Grid.Row="1" Margin="0,0,0,12">
                    <StackPanel.Resources>
                        <Style x:Key="DossierLabel" TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                            <Setter Property="FontSize" Value="11" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Margin" Value="0,0,0,2" />
                        </Style>
                        <Style x:Key="DossierValue" TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                            <Setter Property="FontSize" Value="13" />
                            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas, Courier New" />
                            <Setter Property="Margin" Value="0,0,0,12" />
                        </Style>
                    </StackPanel.Resources>

                    <TextBlock Text="IP ADDRESS" Style="{StaticResource DossierLabel}" />
                    <TextBlock Text="{Binding IpAddress}" Style="{StaticResource DossierValue}" />

                    <TextBlock Text="SESSION PLAYTIME" Style="{StaticResource DossierLabel}" />
                    <TextBlock Text="{Binding Playtime}" Style="{StaticResource DossierValue}" />

                    <TextBlock Text="POSITION" Style="{StaticResource DossierLabel}" />
                    <TextBlock Text="{Binding Position}" Style="{StaticResource DossierValue}" />

                    <TextBlock Text="TOTAL STRIKES" Style="{StaticResource DossierLabel}" />
                    <TextBlock Text="{Binding TotalStrikes}" Style="{StaticResource DossierValue}" />
                </StackPanel>

                <!-- Inspect Inventory Button -->
                <Button Grid.Row="2"
                        Command="{Binding DataContext.InspectInventoryCommand,
                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Style="{StaticResource AccentButton}"
                        Height="32"
                        HorizontalAlignment="Stretch"
                        Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7B8;"
                                   FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                   FontSize="13"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Inspect Inventory"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

                <!-- Inventory Items -->
                <Border Grid.Row="3"
                        Background="{StaticResource InputBackgroundBrush}"
                        CornerRadius="6"
                        Padding="10,8"
                        Margin="0,0,0,12"
                        MaxHeight="200"
                        Visibility="{Binding DataContext.HasInventoryItems,
                                     RelativeSource={RelativeSource AncestorType=UserControl},
                                     Converter={StaticResource BoolToVisConverter}}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <!-- Column Headers -->
                            <Grid Margin="0,0,0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="70" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="ITEM"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="10" FontWeight="SemiBold" />
                                <TextBlock Grid.Column="1" Text="QTY"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="10" FontWeight="SemiBold"
                                           HorizontalAlignment="Right" />
                                <TextBlock Grid.Column="2" Text="SLOT"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="10" FontWeight="SemiBold"
                                           HorizontalAlignment="Right" />
                            </Grid>
                            <ItemsControl ItemsSource="{Binding DataContext.PlayerInventory,
                                                        RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="50" />
                                                <ColumnDefinition Width="70" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding ItemName}"
                                                       Foreground="{StaticResource TextPrimaryBrush}"
                                                       FontFamily="Cascadia Mono, Consolas, Courier New"
                                                       FontSize="11"
                                                       TextTrimming="CharacterEllipsis"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding Quantity}"
                                                       Foreground="{StaticResource AccentBrush}"
                                                       FontFamily="Cascadia Mono, Consolas, Courier New"
                                                       FontSize="11"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding Category}"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       FontSize="10"
                                                       HorizontalAlignment="Right"
                                                       VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Close Button -->
                <Button Grid.Row="4"
                        Content="Close"
                        Command="{Binding DataContext.CloseModalCommand,
                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Style="{StaticResource AccentButton}"
                        Height="32"
                        HorizontalAlignment="Stretch" />
            </Grid>
        </Border>
    </Border>
    </Grid>
</UserControl>
