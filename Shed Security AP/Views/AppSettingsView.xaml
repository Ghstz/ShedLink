<UserControl x:Class="Shed_Security_AP.Views.AppSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="0,0,0,12">
            <TextBlock Text="&#xE713;"
                       FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                       FontSize="18"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0" />
            <TextBlock Text="App Settings"
                       Style="{StaticResource HeaderStyle}"
                       FontSize="20"
                       VerticalAlignment="Center" />
        </StackPanel>

        <!-- Desktop Preferences Card -->
        <Border Grid.Row="1"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                VerticalAlignment="Top">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="&#xE7F4;"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="13"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0" />
                    <TextBlock Text="DESKTOP PREFERENCES"
                               Style="{StaticResource LabelStyle}" />
                </StackPanel>

                <CheckBox IsChecked="{Binding EnableDesktopNotifications}"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          FontSize="14"
                          Cursor="Hand">
                    <TextBlock Text="Enable desktop notifications for security alerts"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                </CheckBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
