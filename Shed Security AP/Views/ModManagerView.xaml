<UserControl x:Class="Shed_Security_AP.Views.ModManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Row -->
        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <!-- Refresh Button -->
            <Button DockPanel.Dock="Right"
                    Style="{StaticResource AccentButton}"
                    Content="Refresh"
                    Command="{Binding RefreshModsCommand}"
                    Height="34"
                    FontSize="12"
                    Padding="16,0" />

            <!-- Upload Button -->
            <Button DockPanel.Dock="Right"
                    Style="{StaticResource AccentButton}"
                    Content="Upload Mod"
                    Command="{Binding UploadModCommand}"
                    Height="34"
                    FontSize="12"
                    Padding="16,0"
                    Margin="0,0,10,0" />

            <!-- Title -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="&#xE74C;"
                           FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                           FontSize="20"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0" />
                <TextBlock Text="Mod Manager"
                           Style="{StaticResource HeaderStyle}"
                           FontSize="20"
                           VerticalAlignment="Center" />
            </StackPanel>
        </DockPanel>

        <!-- Mod List -->
        <Border Grid.Row="1"
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="15">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.2"
                                  ShadowDepth="2" Direction="270" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Column Headers -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="ENABLED"
                               Style="{StaticResource LabelStyle}" />
                    <TextBlock Grid.Column="1"
                               Text="MOD NAME"
                               Style="{StaticResource LabelStyle}" />
                    <TextBlock Grid.Column="2"
                               Text="SIZE"
                               Style="{StaticResource LabelStyle}"
                               TextAlignment="Right" />
                </Grid>

                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Mods}"
                                  x:Name="ModListControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource InputBackgroundBrush}"
                                        CornerRadius="6"
                                        Padding="12,10"
                                        Margin="0,0,0,4"
                                        Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="100" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Toggle CheckBox -->
                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                                  Command="{Binding Tag.ToggleModCommand, RelativeSource={RelativeSource AncestorType=Border}}"
                                                  CommandParameter="{Binding}"
                                                  VerticalAlignment="Center" />

                                        <!-- Mod Name -->
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   FontSize="13"
                                                   VerticalAlignment="Center" />

                                        <!-- Size -->
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Size}"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   VerticalAlignment="Center"
                                                   TextAlignment="Right" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Status Row -->
        <DockPanel Grid.Row="2" Margin="0,10,0,0">
            <StackPanel DockPanel.Dock="Right"
                        Orientation="Horizontal"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}">
                <TextBlock Text="●"
                           Foreground="{StaticResource AccentBrush}"
                           FontSize="10"
                           VerticalAlignment="Center"
                           Margin="0,0,6,0" />
                <TextBlock Text="Loading..."
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12"
                           VerticalAlignment="Center" />
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"
                       VerticalAlignment="Center" />
        </DockPanel>

        <!-- Upload Progress Row -->
        <ProgressBar Grid.Row="3"
                     Style="{StaticResource ModernProgressBar}"
                     Value="{Binding UploadProgress}"
                     Maximum="100"
                     Margin="0,6,0,0"
                     Visibility="{Binding IsUploading, Converter={StaticResource BoolToVisConverter}}" />
    </Grid>
</UserControl>
