<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================
         SHED LINK DASHBOARD — Premium Slate Dark Palette
         Richer darks with subtle warmth, emerald accent.
         ============================================================ -->

    <!-- Background layers (darkest → lightest) -->
    <Color x:Key="BackgroundMediumColor">#FF151519</Color>   <!-- Sidebar / Title bar  -->
    <Color x:Key="BackgroundDarkColor">#FF1C1C21</Color>     <!-- App background       -->
    <Color x:Key="SurfaceColor">#FF232329</Color>            <!-- Cards / popovers     -->
    <Color x:Key="InputBackgroundColor">#FF151519</Color>    <!-- Inset input fields   -->

    <!-- Accent — Emerald Green -->
    <Color x:Key="AccentColor">#FF10B981</Color>
    <Color x:Key="AccentHoverColor">#FF059669</Color>

    <!-- Text -->
    <Color x:Key="TextPrimaryColor">#FFFAFAFA</Color>
    <Color x:Key="TextSecondaryColor">#FFA1A1AA</Color>

    <!-- Utility -->
    <Color x:Key="BorderSubtleColor">#FF33333B</Color>
    <Color x:Key="CloseHoverColor">#FFE53935</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="BackgroundDarkBrush"     Color="{StaticResource BackgroundDarkColor}" />
    <SolidColorBrush x:Key="BackgroundMediumBrush"   Color="{StaticResource BackgroundMediumColor}" />
    <SolidColorBrush x:Key="SurfaceBrush"            Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="CardBackgroundBrush"     Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="InputBackgroundBrush"    Color="{StaticResource InputBackgroundColor}" />
    <SolidColorBrush x:Key="AccentBrush"             Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentHoverBrush"        Color="{StaticResource AccentHoverColor}" />
    <SolidColorBrush x:Key="TextPrimaryBrush"        Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush"      Color="{StaticResource TextSecondaryColor}" />
    <SolidColorBrush x:Key="BorderSubtleBrush"       Color="{StaticResource BorderSubtleColor}" />
    <SolidColorBrush x:Key="CloseHoverBrush"         Color="{StaticResource CloseHoverColor}" />

    <!-- ============================================================
         TYPOGRAPHY
         ============================================================ -->

    <FontFamily x:Key="AppFont">Segoe UI, Inter, sans-serif</FontFamily>

    <!-- Header — page / section titles -->
    <Style x:Key="HeaderStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>

    <!-- Label — field labels, uppercase convention -->
    <Style x:Key="LabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource AppFont}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
    </Style>

</ResourceDictionary>
